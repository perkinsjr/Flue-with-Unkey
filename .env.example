# Provider API keys — set whichever models your agents use.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Optional: override the port the Flue server listens on.
# Unkey Deploy injects PORT=8080 at runtime, so you don't normally
# need to set this in production.
# PORT=3000
